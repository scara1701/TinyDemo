<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TinyDemo.WinUIClient.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TinyDemo.WinUIClient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:TinyDemo.WinUIClient.Controls"
    mc:Ignorable="d"
    
    Title="Lotto Picker">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="110" />
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <controls:NumberControl Number="{Binding Lotto.Numbers[0], Mode=OneWay}" Width="75" Height="75"/>
            <controls:NumberControl Number="{Binding Lotto.Numbers[1], Mode=OneWay}" Width="75" Height="75"/>
            <controls:NumberControl Number="{Binding Lotto.Numbers[2], Mode=OneWay}" Width="75" Height="75"/>
            <controls:NumberControl Number="{Binding Lotto.Numbers[3], Mode=OneWay}" Width="75" Height="75"/>
            <controls:NumberControl Number="{Binding Lotto.Numbers[4], Mode=OneWay}" Width="75" Height="75"/>
            <controls:NumberControl Number="{Binding Lotto.Numbers[5], Mode=OneWay}" Width="75" Height="75"/>
            <TextBlock Text="+" FontSize="28" FontWeight="Bold"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <controls:NumberControl Number="{Binding Lotto.Numbers[6], Mode=OneWay}" Width="75" Height="75"/>
        </StackPanel>
        <Button Content="Generate new lotto numbers" Grid.Row="1"  Margin="10" FontSize="28" Command="{Binding GenerateCommand}" IsEnabled="{Binding NotBusy, Mode=OneWay}" />
        <TextBlock Text="{Binding StatusMessage, Mode=OneWay}" Grid.Row="2" Margin="10" HorizontalAlignment="Center" />

    </Grid>
</Window>
